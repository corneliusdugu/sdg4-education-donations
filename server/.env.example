# Server configuration (Codio Box URL expects 8080)
PORT=8080

# MongoDB Atlas connection string (cloud ONLY)
MONGO_URI=mongodb+srv://cdugu:Computer0070@cluster0.4ogcivt.mongodb.net/sdg4_education_donations?retryWrites=true&w=majority

# JWT secret
JWT_SECRET=replace_with_a_long_random_secret

# Optional: allow Codio frontend origin during development
CODIO_CLIENT_ORIGIN=
